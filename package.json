{
  "name": "dmirror",
  "description": "Mirror a directory to another directory in the filesystem or FTP (like a Raid but without blocking), you can plug your own if other target is needed.",
  "version": "0.0.1",
  "author": {
    "name": "Luis Lafuente, Noboxout.",
    "email": "llafuente@noboxout.com"
  },
  "maintainers": [
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/llafuente/dmirror.git"
  },
  "keywords": [
    "mirror",
    "raid",
    "synchronization",
    "filesystem",
    "mirroring",
    "directory",
    "mirrorfolder",
    "rsync"
  ],
  "directories": {
    "lib": ".",
    "test": "test"
  },
  "scripts" : {
    "test" : "tap test/*.js"
  },
  "dependencies": {
    "jsftp": ">=0.5.1",
    "node-class": ">=0.0.24",
    "winston": ">=0.6.2",
    "rimraf": ">=2.1.4"
  },
  "devDependencies": {
    "tap": ">=0.3.2"
  },
  "main": "./index.js",
  "engines": {
    "node": ">= 0.8.0"
  },
  "readme": "# dmirror\nMirror a directory to another directory in the filesystem or FTP (like a Raid but without blocking)"
}
